# Library & Utilities

This folder stores application-wide utility functions and integrations.

- `firebase.ts`: Holds the Firebase App initialization and Firestore configuration, including real-time listeners and operation types.
- `utils.ts`: Contains shared helper functions like the `cn` exact class merge utility for Tailwind CSS.
